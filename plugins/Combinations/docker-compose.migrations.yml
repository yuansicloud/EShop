version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=Combinations_Identity
      - Combinations_DB=Combinations_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
